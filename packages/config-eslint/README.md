# @timmbr/eslint-config

Shared ESLint rules and configuration for React, TypeScript, and monorepo packages across the Timmbr Design System.

## Usage

In any package's `.eslintrc.js`:

```javascript
module.exports = {
  root: true,
  extends: ["@timmbr/eslint-config"],
};
```
